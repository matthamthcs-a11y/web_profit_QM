# Supabase is optional in local dev because lib/data falls back to mock data
# when these variables are missing. Set real values in .env.local and Vercel.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
